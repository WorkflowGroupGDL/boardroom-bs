"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const http = __importStar(require("http"));
const form_data_1 = __importDefault(require("form-data"));
const index_1 = require("../../index");
const http_1 = require("../../codegen/crm/contacts/http/http");
const isomorphic_fetch_1 = require("../../codegen/crm/contacts/http/isomorphic-fetch");
const configuration_1 = require("../../codegen/files/configuration");
const FilesApi_1 = require("../../codegen/files/apis/FilesApi");
const isomorphic_fetch_2 = require("../../codegen/files/http/isomorphic-fetch");
const servers_1 = require("../../codegen/files/servers");
describe('HTTP transport', () => {
    it('sends apiRequest JSON bodies through native fetch', () => __awaiter(void 0, void 0, void 0, function* () {
        let capturedRequest;
        const server = yield createServer((request, response) => __awaiter(void 0, void 0, void 0, function* () {
            capturedRequest = request;
            response.statusCode = 201;
            response.setHeader('content-type', 'application/json');
            response.end(JSON.stringify({ ok: true }));
        }));
        try {
            const client = new index_1.Client({ accessToken: 'test-token' });
            const response = yield client.apiRequest({
                method: 'POST',
                overlapUrl: `${server.baseUrl}/json?foo=bar`,
                body: { hello: 'world' },
            });
            expect(response.status).toBe(201);
            expect(yield response.json()).toEqual({ ok: true });
            expect(capturedRequest).toBeDefined();
            const req = capturedRequest;
            expect(req.method).toBe('POST');
            expect(req.url).toBe('/json?foo=bar');
            expect(req.headers.authorization).toBe('Bearer test-token');
            expect(String(req.headers['content-type'])).toContain('application/json');
            expect(req.body.toString()).toBe(JSON.stringify({ hello: 'world' }));
        }
        finally {
            yield server.close();
        }
    }));
    it('preserves multipart apiRequest uploads with the form-data package', () => __awaiter(void 0, void 0, void 0, function* () {
        let capturedRequest;
        const server = yield createServer((request, response) => __awaiter(void 0, void 0, void 0, function* () {
            capturedRequest = request;
            response.statusCode = 204;
            response.end();
        }));
        try {
            const formData = new form_data_1.default();
            formData.append('field', 'value');
            formData.append('file', Buffer.from('payload'), { filename: 'hello.txt', contentType: 'text/plain' });
            const client = new index_1.Client();
            const response = yield client.apiRequest({
                method: 'POST',
                overlapUrl: `${server.baseUrl}/upload`,
                body: formData,
                defaultJson: false,
            });
            expect(response.status).toBe(204);
            expect(capturedRequest).toBeDefined();
            const req = capturedRequest;
            expect(String(req.headers['content-type'])).toContain('multipart/form-data; boundary=');
            expect(req.body.toString()).toContain('name="field"');
            expect(req.body.toString()).toContain('value');
            expect(req.body.toString()).toContain('filename="hello.txt"');
            expect(req.body.toString()).toContain('payload');
        }
        finally {
            yield server.close();
        }
    }));
    it('returns generated ResponseContext bodies through native fetch', () => __awaiter(void 0, void 0, void 0, function* () {
        let capturedRequest;
        const server = yield createServer((request, response) => __awaiter(void 0, void 0, void 0, function* () {
            capturedRequest = request;
            response.statusCode = 202;
            response.setHeader('content-type', 'application/octet-stream');
            response.end(Buffer.from('generated-response'));
        }));
        try {
            const requestContext = new http_1.RequestContext(`${server.baseUrl}/generated`, http_1.HttpMethod.POST);
            requestContext.setHeaderParam('X-Test', '1');
            requestContext.setBody('payload');
            const response = yield new isomorphic_fetch_1.IsomorphicFetchHttpLibrary().send(requestContext).toPromise();
            expect(response.httpStatusCode).toBe(202);
            expect(capturedRequest).toBeDefined();
            const req = capturedRequest;
            expect(req.method).toBe('POST');
            expect(req.headers['x-test']).toBe('1');
            expect(req.body.toString()).toBe('payload');
            expect(yield response.body.text()).toBe('generated-response');
            expect((yield response.body.binary()).toString()).toBe('generated-response');
        }
        finally {
            yield server.close();
        }
    }));
    it('supports generated multipart uploads when a custom httpAgent is configured', () => __awaiter(void 0, void 0, void 0, function* () {
        let capturedRequest;
        const server = yield createServer((request, response) => __awaiter(void 0, void 0, void 0, function* () {
            capturedRequest = request;
            response.statusCode = 200;
            response.end('ok');
        }));
        try {
            const configuration = (0, configuration_1.createConfiguration)({
                baseServer: new servers_1.ServerConfiguration(server.baseUrl, {}),
            });
            const requestContext = yield new FilesApi_1.FilesApiRequestFactory(configuration).upload({ data: Buffer.from('payload'), name: 'hello.txt' }, undefined, '/folder', 'hello.txt');
            requestContext.setAgent(new http.Agent({ keepAlive: true }));
            const response = yield new isomorphic_fetch_2.IsomorphicFetchHttpLibrary().send(requestContext).toPromise();
            expect(response.httpStatusCode).toBe(200);
            expect(capturedRequest).toBeDefined();
            const req = capturedRequest;
            expect(req.method).toBe('POST');
            expect(req.url).toBe('/files/v3/files');
            expect(String(req.headers['content-type'])).toContain('multipart/form-data; boundary=');
            expect(req.body.toString()).toContain('filename="hello.txt"');
            expect(req.body.toString()).toContain('name="folderPath"');
            expect(req.body.toString()).toContain('/folder');
            expect(req.body.toString()).toContain('payload');
            expect(yield response.body.text()).toBe('ok');
        }
        finally {
            yield server.close();
        }
    }));
});
function createServer(handler) {
    return __awaiter(this, void 0, void 0, function* () {
        const server = http.createServer((request, response) => __awaiter(this, void 0, void 0, function* () {
            const bodyChunks = [];
            request.on('data', (chunk) => {
                bodyChunks.push(Buffer.isBuffer(chunk) ? chunk : Buffer.from(chunk));
            });
            request.on('end', () => __awaiter(this, void 0, void 0, function* () {
                var _a, _b;
                yield handler({
                    body: Buffer.concat(bodyChunks),
                    headers: request.headers,
                    method: (_a = request.method) !== null && _a !== void 0 ? _a : 'GET',
                    url: (_b = request.url) !== null && _b !== void 0 ? _b : '/',
                }, response);
            }));
        }));
        yield new Promise((resolve) => {
            server.listen(0, '127.0.0.1', () => resolve(undefined));
        });
        const address = server.address();
        if (!address || typeof address === 'string') {
            throw new Error('Failed to start test server.');
        }
        return {
            baseUrl: `http://127.0.0.1:${address.port}`,
            close: () => __awaiter(this, void 0, void 0, function* () {
                yield new Promise((resolve, reject) => {
                    server.close((error) => {
                        if (error) {
                            reject(error);
                            return;
                        }
                        resolve(undefined);
                    });
                });
            }),
        };
    });
}
//# sourceMappingURL=httpTransport.spec.js.map