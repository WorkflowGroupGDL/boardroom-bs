# Example environment variables for EdgeOne deployment
# Copy this to .env and fill with real values before deploying (do NOT commit secrets to git)

# JWT
JWT_SECRET=boardroom_bs_executive_secret_key_2026
JWT_EXPIRES_IN=8h

# HubSpot (optional if you integrate with HubSpot)
HUBSPOT_ACCESS_TOKEN=

# Port (used for local development)
PORT=3000

# Any other secrets your app needs
# EXAMPLE_API_KEY=
